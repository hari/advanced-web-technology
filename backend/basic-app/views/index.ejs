<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>awt.link</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; background: #fafbfc; color: #222; margin: 0; padding: 0; }
    header { padding: 2rem 0 1rem 0; text-align: center; }
    nav a { margin: 0 1rem; color: #38f; text-decoration: none; font-weight: 500; }
    nav a:hover { color: #111; }
    main { max-width: 400px; margin: 1rem auto; background: #fff; border-radius: 16px; box-shadow: 0 2px 16px #0001; padding: 2rem; text-align: center; }
    main p { color: #666; }
  </style>
</head>
<body>
  <header>
    <h1>awt.link</h1>
  </header>
  <main>
    <h2>Welcome to awt.link</h2>
    <p>One link for all your vibes. Share links, get anonymous feedback, and more.</p>

    <nav>
      <% if (user) { %>
        <a href="/profile">Profile</a>
        <a href="/auth/logout">Logout</a>
      <% } else { %>
        <a href="/auth/login">Login</a>
        <a href="/auth/register">Register</a>
      <% } %>
    </nav>
  </main>
</body>
</html>